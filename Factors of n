#include <stdio.h>
int main()
{
    int number;
    printf("Enter a number: ");
    scanf("%d", &number);
    printf("\nFactors of %d are: ", number);
    int i = 1;
    do
    {
        if (number % i == 0)
        {
            printf("%d ", i);
        }
        
        i++;
    } while (i <= number);

    return 0;
}
